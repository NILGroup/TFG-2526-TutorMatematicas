<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:MathTutor.PageModels"             
             xmlns:models="clr-namespace:MathTutor.Models"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MathTutor.Pages.MainPage"
             x:DataType="pageModels:MainPageModel"
             x:Name="OverviewPage"
             Title="Today">

    <ContentPage.Behaviors>
        <!-- Automatically call commands on navigation events -->
        <toolkit:EventToCommandBehavior EventName="Appearing"
                                        Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ScrollView>
        <StackLayout Padding="16" Spacing="12">
            <Label Text="Recomendaciones de hoy"
                   FontSize="24"
                   FontAttributes="Bold" />

            <CollectionView ItemsSource="{Binding RecommendedProblems}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,4"
                                Stroke="#e0e0e0"
                                Padding="10">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="8" />
                            </Border.StrokeShape>
                            <StackLayout>
                                <Label Text="{Binding Statement}" FontAttributes="Bold" />
                                <Label Text="Curso: {Binding Course}" FontSize="12" />
                                <Label Text="Tema: {Binding Kc}" FontSize="12" />
                                <Label Text="Dificultad: {Binding Difficulty}" FontSize="12" />
                                <Button Text="Empezar"
                                        Command="{Binding BindingContext.StartProblemCommand, Source={x:Reference Name=MainPage}}"
                                        CommandParameter="{Binding Id}" />
                            </StackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="Continuar sesión"
                    IsVisible="{Binding HasSession}"
                    Command="{Binding ContinueSessionCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>
