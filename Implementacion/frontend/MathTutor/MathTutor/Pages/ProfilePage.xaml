<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:MathTutor.PageModels"
             xmlns:models="clr-namespace:MathTutor.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MathTutor.Pages.ProfilePage"
             x:Name="MetaPage"
             Title="Profile">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior EventName="Appearing"
                                        Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ScrollView>
        <StackLayout Padding="16" Spacing="12">
            <Label Text="Perfil" FontSize="24" FontAttributes="Bold" />
            <Grid ColumnDefinitions="*,*,*,*" RowDefinitions="Auto">
                <StackLayout Grid.Column="0">
                    <Label Text="Completados" FontAttributes="Bold" />
                    <Label Text="{Binding CompletedProblems}" />
                </StackLayout>
                <StackLayout Grid.Column="1">
                    <Label Text="Totales" FontAttributes="Bold" />
                    <Label Text="{Binding TotalProblems}" />
                </StackLayout>
                <StackLayout Grid.Column="2">
                    <Label Text="Precisión" FontAttributes="Bold" />
                    <Label Text="{Binding AccuracyPct}" />
                </StackLayout>
                <StackLayout Grid.Column="3">
                    <Label Text="Racha" FontAttributes="Bold" />
                    <Label Text="{Binding Streak}" />
                </StackLayout>
            </Grid>

            <CollectionView ItemsSource="{Binding TopicProgress}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Margin="0,8">
                            <Label Text="{Binding TopicName}" FontAttributes="Bold" />
                            <ProgressBar Progress="{Binding Progress}" HeightRequest="8" />
                            <Label Text="{Binding ProgressMessage}" FontSize="10" />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="Repetir intereses" Command="{Binding RepeatInterestsCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>